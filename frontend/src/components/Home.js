// Home.js
import ImageAddService from "../services/ImageAddService";

const Home = () => {

  return (
    <div className='hero'>
      <section className='container' id='image-add'>
      </section>
    </div>
  );
};

export default Home;
